<!--/**
    Navbar view handlebars template file.
 */-->

<div id="topNavBar" class="navbar">
    <div class="navbar-inner no-border">
        <div class="navbar-form">
            {{#linkTo index}}
            <div style="float: left;">
                <img src="img/logosmall.png">
            </div>
            <div class="brand">urban<strong>fruit.ly</strong></div>
            {{/linkTo}}
            <ul class="nav pull-right">
                <li class="divider-vertical"></li>
                <li>
                    {{log isLoggedIn}}
                    {{#if isLoggedIn}}
                        <button {{action logout}} class="btn">Logout</button>
                    {{else}}
                        {{#linkTo login tag="button" class="btn btn-danger"}}Login{{/linkTo}}
                    {{/if}}

                </li>
            </ul>
        </div>
    </div>
</div>